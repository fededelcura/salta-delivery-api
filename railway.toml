# Railway — API Salta Delivery
# En el dashboard: Root Directory = api (o deploy desde esta carpeta)

[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
